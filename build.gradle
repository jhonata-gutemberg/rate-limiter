plugins {
	id 'java'
}

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(21)
	}
}

subprojects {
	apply plugin: 'java'

	group = 'dev.gutemberg'
	version = '0.0.1-SNAPSHOT'

	repositories {
		mavenCentral()
	}
}

tasks.named('test') {
	useJUnitPlatform()
}
